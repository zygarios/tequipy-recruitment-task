import { GenericFunctionPipe } from './generic-function.pipe';

describe(' ', () => {
  it('create an instance', () => {
    const pipe = new GenericFunctionPipe();
    expect(pipe).toBeTruthy();
  });
});
